<?php

declare(strict_types=1);

namespace Simplex;

if (! function_exists('Simplex\test')) { 
    /**
     * Helper for defining a value.
     *
     * @param mixed $value
     */
    function test() : null
    {
        r('test');
    }
}
