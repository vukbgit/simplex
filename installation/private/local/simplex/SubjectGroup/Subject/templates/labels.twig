{# included without context passing labels global variable  #}
{% set singular = 'SUBJECT-SINGULAR'|trans %}
{% set labels = setLabels('SUBJECT-KEY', {
  'subject': 'SUBJECT-LABEL'|trans,
  'subject-record-menu-title': '%s "%%s":'|format(singular),
  'fields': {
    'FIELD-NAME': 'FIELD-LABEL'|trans,
  }
}) %}
{# to override global actions labels #}
{#% set labels = setLabels('SUBJECT-KEY', {
  'actions': {
    'GLOBAL-ACTION-KEY': 'overridden label'
  }
}) %#}
{# to override global alerts labels #}
{#% set labels = setLabels('SUBJECT-KEY', {
  'alerts': {
    'GLOBAL-ALERT-KEY': 'overridden label'
  }
}) %#}
{# for record actions defined into navigation.php placeholders enclosed by curly brackets {} will be substituded by field values found into record #}
{#% set labels = setLabels('actions', {
  'SUBJECT-ACTION-KEY': 'ACTION-LABEL ({FIELD-NAME})'|trans
}) %#}
