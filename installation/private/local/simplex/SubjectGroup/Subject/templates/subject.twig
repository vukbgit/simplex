{# extends local area template #}
{% extends pathToAreaTemplate %}
{# subject labels #}
{% set subjectLabel = 'SUBJECT-LABEL'|trans %}

{# labels for model fields #}
{% set fieldsLabels = {
    'FIELD-NAME': 'FIELD-LABEL'|trans,
} %}
